export enum MessageStatus {
    DELIVERED = 'DELIVERED',
    READ = 'READ',
    SENT = 'SENT',
    FAILED = 'FAILED'
  }
  
  // You can expand this mock as needed to match your actual Prisma schema
  export type PrismaClient = unknown